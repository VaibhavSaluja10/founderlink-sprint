spring:
  application:
    name: messaging-service
  datasource:
    url: jdbc:mysql://localhost:3306/founderlink_messaging_sprint?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  config:
    import: optional:configserver:http://localhost:8887

server:
  port: 8066

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/

jwt:
  secret: 9a4f2c8d3b7a1e6f45c8a0b3f267d8b1d4e6f3c8a9d2b5f8e3a9c8b5f6v8a3d9

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
