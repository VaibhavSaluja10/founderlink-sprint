spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://mysql-db:3306/founderlink_auth_sprint?createDatabaseIfNotExist=true
    # url: jdbc:mysql://localhost:3306/founderlink_auth_sprint?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  cloud:
    discovery:
      enabled: true
  config:
    import: optional:configserver:http://localhost:8887

server:
  port: 8061

eureka:
  client:
    enabled: true

jwt:
  secret: 9a4f2c8d3b7a1e6f45c8a0b3f267d8b1d4e6f3c8a9d2b5f8e3a9c8b5f6v8a3d9
  expirationMs: 3600000
