# Team Service - Development Profile
spring:
  datasource:
    url: jdbc:mysql://mysql-db:3306/founderlink_team_sprint?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  rabbitmq:
    host: rabbitmq
    port: 5672

logging:
  level:
    org.capgemini: DEBUG
    org.springframework.amqp: DEBUG
