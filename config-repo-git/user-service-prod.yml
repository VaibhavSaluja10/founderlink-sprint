# User Service - Production Profile
spring:
  datasource:
    url: jdbc:mysql://prod-db-server:3306/founderlink_user?createDatabaseIfNotExist=false
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    org.capgemini: WARN
